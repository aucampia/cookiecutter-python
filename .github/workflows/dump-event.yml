name: dump-event
on: pull_request_target

jobs:
  dump-event:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Dumping event payload:"
          echo "${{ toJson(github.event) }}"
          echo "Dumping environment variables:"
          echo "${{ toJson(env) }}"
