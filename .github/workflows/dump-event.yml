name: dump-event
on: pull_request_target

jobs:
  dump-event:
    runs-on: ubuntu-latest
    steps:
      - run: |
          cat <<'EOF'

          Event payload:
          ${{ toJson(github.event) }}

          Environment variables:
          ${{ toJson(env) }}
          EOF
